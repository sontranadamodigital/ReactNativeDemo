import * as React from 'react'
import { Text as ReactNativeText, TextProps as TextProperties, TextStyle, Platform, ViewStyle } from 'react-native'
import { Color as valueColor, FontSize } from '../../assets'
import { I18n } from '../../instances'

export interface CommonAlertProps {

}

export function CommonAlert(props: CommonAlertProps) {
    const { ...rest } = props
    return (
        <>
        </>
    )
}
